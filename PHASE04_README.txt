PHASE 4 - JAVASCRIPT UPDATE

Copy the contents of this folder into the root of your existing qselmer.github.io repository.
Allow the listed files to be replaced, but do not delete or replace your .git directory.

Then run:
  npm ci
  npm test
  git status
  git add .
  git commit -m "Optimize and validate JavaScript build"
  git push origin master
